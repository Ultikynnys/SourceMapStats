# Steam Web API key for fetching game server lists
# Get one free at https://steamcommunity.com/dev/apikey
STEAM_API_KEY=your-steam-api-key-here

# Game to track (gamedir value)
# Options: tf, csgo, cs2, cstrike, dod, hl2dm, l4d, l4d2
GAME_DIR=tf

# Admin panel IP whitelist (comma-separated)
# Only these IPs can access http://yourserver:5000/admin
# Admin panel IP whitelist (comma-separated)
# Only these IPs can access http://yourserver:5000/admin
# Leave empty to disable admin panel entirely
# Example with multiple IPs: 127.0.0.1,192.168.1.100,176.57.188.166
ADMIN_IPS=127.0.0.1

# Certbot / HTTPS Configuration
DOMAIN_NAME=your-domain.com
CERTBOT_EMAIL=your-email@example.com
# Path to Nginx webroot for ACME challenges
# Linux default: /usr/share/nginx/html or /var/www/html
# Windows default: C:\nginx\html (check your nginx.conf)
CERTBOT_WEBROOT=/usr/share/nginx/html
